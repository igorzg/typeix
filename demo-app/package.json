{
  "name": "demo-app",
  "version": "0.0.1",
  "scripts": {
    "postinstall": "typings install",
    "compile": "tsc -p tsconfig.json",
    "demo": "npm run compile && node build/bootstrap.js"
  },
  "dependencies": {
    "typeix": "^1.0.0-alpha-4",
    "typescript": "^2.0.3",
    "typings": "^1.4.0"
  }
}
